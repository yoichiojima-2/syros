__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/
.terraform/
*.tfstate
*.tfstate.*
# .terraform.lock.hcl is deliberately tracked: it pins exact provider versions
# so every machine and CI resolve identically. State itself lives in GCS.
scratch/**
!scratch/README.md

console/node_modules/
console/.next/
console/out/
.mypy_cache

# Built console bundle (generated by `make console`; built in Docker for deploys)
src/syros/console/static/
